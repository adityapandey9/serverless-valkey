const std = @import("std");
const l_list = @import("commands/l_list.zig");

export fn processZigCommand() !void {
    l_list.
}

// test "basic add functionality" {
//     try testing.expect(add(3, 7) == 10);
// }
